# Force Elastic Beanstalk to use port 8080 for the application.
# Nginx proxies port 80 -> 127.0.0.1:8080; health checks and proxy upstream use this port.
option_settings:
  aws:elasticbeanstalk:application:environment:
    PORT: "8080"
